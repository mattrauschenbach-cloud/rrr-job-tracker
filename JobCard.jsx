import {navItems} from "../data";
export default function Sidebar({page,setPage}){const bottom=["Dashboard","Jobs","Daily Log","Materials","Payroll"];return <><aside className="sidebar"><div className="brand"><div className="logo">RRR</div><div><h1>OS</h1><p>Remodeling Command Center</p></div></div><nav className="sideNav">{navItems.map(i=><button key={i} className={page===i?"navBtn active":"navBtn"} onClick={()=>setPage(i)}>{i}</button>)}</nav></aside><nav className="bottomNav">{bottom.map(i=><button key={i} className={page===i?"bottomBtn bottomActive":"bottomBtn"} onClick={()=>setPage(i)}><span>{iconFor(i)}</span><small>{i==="Daily Log"?"Log":i}</small></button>)}</nav></>}
function iconFor(i){return i==="Dashboard"?"🏠":i==="Jobs"?"📁":i==="Daily Log"?"⏱":i==="Materials"?"📦":i==="Payroll"?"💰":"•";}
