export default function Materials({jobs,materialEntry,setMaterialEntry,addMaterial,materials}){return <section className="panel"><h3>Materials</h3><p>Track supplier runs, receipts, and material costs by job.</p><form className="formGrid" onSubmit={addMaterial}><select value={materialEntry.jobId} onChange={e=>setMaterialEntry({...materialEntry,jobId:e.target.value})}><option value="">Select job</option>{jobs.map(j=><option key={j.id} value={j.id}>{j.name}</option>)}</select><input placeholder="Supplier" value={materialEntry.supplier} onChange={e=>setMaterialEntry({...materialEntry,supplier:e.target.value})}/><input placeholder="Item / description" value={materialEntry.item} onChange={e=>setMaterialEntry({...materialEntry,item:e.target.value})}/><input type="number" placeholder="Cost" value={materialEntry.cost} onChange={e=>setMaterialEntry({...materialEntry,cost:e.target.value})}/><input placeholder="Notes" value={materialEntry.notes} onChange={e=>setMaterialEntry({...materialEntry,notes:e.target.value})}/><button className="primaryBtn">Save Material</button></form><h3 className="sectionTitle">Recent Materials</h3><div className="listStack">{materials.length===0?<div className="emptyState">No materials yet.</div>:materials.slice(0,10).map(e=>{const j=jobs.find(x=>x.id===e.jobId);return <div className="listItem" key={e.id}><strong>{e.item} — ${Number(e.cost||0).toLocaleString()}</strong><span>{e.supplier||"No supplier"} | {j?.name||"No job"} | {e.notes||""}</span></div>})}</div></section>}
