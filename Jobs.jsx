export default function JobCard({job,onOpen,onDelete}){const cost=Number(job.labor||0)+Number(job.materials||0);const profit=Number(job.price||0)-cost;return <div className="jobCard"><button className="jobCardButton" type="button" onClick={()=>onOpen?.(job)}><div className="jobTop"><div><h4>{job.name||"Unnamed Job"}</h4><p>{job.customer||"No customer added"}</p></div><span className="statusBadge">{job.status||"In Progress"}</span></div><p className="address">{job.address||"No address added"}</p><div className="moneyGrid"><div><span>Contract</span><strong>${Number(job.price||0).toLocaleString()}</strong></div><div><span>Cost</span><strong>${cost.toLocaleString()}</strong></div><div><span>Profit</span><strong className={profit>=0?"goodMoney":"badMoney"}>${profit.toLocaleString()}</strong></div></div></button>{onDelete&&<button className="dangerBtn" onClick={()=>onDelete(job.id)}>Delete</button>}</div>}
