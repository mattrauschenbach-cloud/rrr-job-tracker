export default function Workers({workers,newWorker,setNewWorker,addWorker,removeWorker}){return <section className="panel"><h3>Add Worker</h3><form className="formGrid" onSubmit={addWorker}><input placeholder="Worker name" value={newWorker.name} onChange={e=>setNewWorker({...newWorker,name:e.target.value})}/><input type="number" placeholder="Hourly rate" value={newWorker.rate} onChange={e=>setNewWorker({...newWorker,rate:e.target.value})}/><select value={newWorker.status} onChange={e=>setNewWorker({...newWorker,status:e.target.value})}><option>Active</option><option>Inactive</option></select><button className="primaryBtn">Save Worker</button></form><h3 className="sectionTitle">Workers</h3>{workers.length===0?<div className="emptyState">No workers yet.</div>:<div className="jobGrid">{workers.map(w=><div className="jobCard" key={w.id}><div className="jobTop"><div><h4>{w.name}</h4><p>${Number(w.rate||0).toFixed(2)} / hr</p></div><span className="statusBadge">{w.status}</span></div><button className="dangerBtn" onClick={()=>removeWorker(w.id)}>Delete</button></div>)}</div>}</section>}
