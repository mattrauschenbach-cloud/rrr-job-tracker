import JobCard from "../components/JobCard";
export default function Dashboard({jobs,totals,openJob,workers,timeEntries}){const today=new Date().toISOString().slice(0,10);const logs=timeEntries.filter(e=>e.date===today);return <><section className="heroPanel"><div><span className="eyebrow">Command Center</span><h3>Good Morning, Matt</h3><p>One screen for jobs, crew, payroll, and profit.</p></div><div className="heroMoney"><span>Estimated Profit</span><strong>${totals.profit.toLocaleString()}</strong></div></section><section className="statsGrid"><div className="statCard"><span>Active Jobs</span><strong>{jobs.length}</strong></div><div className="statCard"><span>Workers</span><strong>{workers.length}</strong></div><div className="statCard"><span>Logs Today</span><strong>{logs.length}</strong></div><div className="statCard profit"><span>Contracts</span><strong>${totals.contractTotal.toLocaleString()}</strong></div></section><section className="panel"><h3>Active Jobs</h3><p>Tap a job to open its workspace.</p>{jobs.length===0?<div className="emptyState"><h4>No jobs yet</h4><p>Add your first job from the Jobs page.</p></div>:<div className="jobGrid">{jobs.slice(0,4).map(j=><JobCard key={j.id} job={j} onOpen={openJob}/>)}</div>}</section></>}
